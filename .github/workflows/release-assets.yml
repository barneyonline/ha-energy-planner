name: Release Assets

on:
  release:
    types:
      - published

permissions:
  contents: write

jobs:
  package-component:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Validate release tag matches manifest version
        env:
          RELEASE_TAG: ${{ github.ref_name }}
        run: |
          python - <<'PY'
          import json
          import os
          from pathlib import Path

          version = json.loads(
              Path("custom_components/ha_energy_planner/manifest.json").read_text(encoding="utf-8")
          )["version"]
          tag = os.environ["RELEASE_TAG"].removeprefix("v")
          if version != tag:
              raise SystemExit(
                  f"Release tag {os.environ['RELEASE_TAG']} does not match manifest version {version}"
              )
          PY
      - name: Create component archive
        env:
          RELEASE_TAG: ${{ github.ref_name }}
        run: |
          zip -r "ha-energy-planner-${RELEASE_TAG}.zip" custom_components/ha_energy_planner \
            -x "*/__pycache__/*" "*/.mypy_cache/*" "*/.pytest_cache/*"
      - name: Upload release asset
        uses: softprops/action-gh-release@v3
        with:
          files: ha-energy-planner-${{ github.ref_name }}.zip
